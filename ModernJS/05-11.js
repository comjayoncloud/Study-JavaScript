/** 변수 선언문은 값으로 평가될 수 없으므로 표현식이 아니다. */
var x;

// 1, 2, 1 + 2, x = 1 + 2 는 모두 표현식 (왜냐면 값으로 평가되기 때문)

/** x = 1 + 2 는 표현식이면서 완전한 문이기도 하다 */
x = 1 + 2;

// 표현식? 값으로 평가될 수 있는 문
// 문 ? 프로그램을 구성하는 기본 단위 이자 최소 실행 단위
