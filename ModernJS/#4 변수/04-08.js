console.log(score); // 2번으로 실행. 현재 score은 undefined

var score; // 1번으로 실행 . 즉 score은 undefined
score = 80; // 3번으로 실행 . score 에 값을 할당 해주었음. score의 값은 undefined -> 80

console.log(score); // 4번으로 실행 . score의 값 80이 나옴
