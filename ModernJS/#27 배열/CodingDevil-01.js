/** splice : 특정 요소 지움 , 지우고 추가 , 지우지 않고 추가 , 삭제된 요소 반환 */

/** slice(n,m) : n부터 m 까지반환 . 괄호안에 아무것도 안넣으면 배열이 복사. */

/** concat : 합쳐서 새배열 반환. */

/** forEach : 배열 반복 */

/** indexOf, lastIndexOf */

/** includes() : 포함하는지 확인 */

/** find,findIndex(fn) : 첫번째 true 값만 반환하고 끝. 없으면 undefined */

/** filter(fn) : 만족하는 모든 요소를 배열로 반환 */

/** reverse() : 역순으로 재정렬 */

/** map(fn) : 함수를 받아 특정기능을 시행하고 새로운 배열을 반환 */

/** join : 하나의 문자열로 만들어줌 */

/** split : 문자열을 나눠서 배열로 만들어줌 */

/** Array.isArray() : 배열인지 확인하는법 */
